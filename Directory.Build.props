<Project>
  <PropertyGroup>
    <!-- Target net8.0; if 8 is missing, use a newer shared runtime (9/10) so the app still launches. -->
    <RollForward>LatestMajor</RollForward>
  </PropertyGroup>
</Project>
